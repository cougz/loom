#!/usr/bin/env bash
# Manual deploy — only for the first deploy before Workers Builds is
# wired up. After that, push to main and let Cloudflare build + deploy.
set -euo pipefail
pnpm --filter @loom/web deploy
pnpm --filter @loom/outbound deploy

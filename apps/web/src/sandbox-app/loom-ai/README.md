loom-ai
=======

Thin CLI wrapper that posts to the Worker's framework-level AI helper
endpoint. Uses Workers AI by default; the user's configured provider
key is picked up from OpenCode's config when present.

M3 will replace this placeholder with a small Go binary.

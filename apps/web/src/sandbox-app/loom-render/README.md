loom-render
===========

Thin CLI wrapper over the Worker's Browser Rendering helper. Supports
`screenshot`, `pdf`, `scrape`, `extract` verbs. Large outputs are
written to R2 and the CLI prints the resulting URL.

M3 will replace this placeholder with a small Go binary.
